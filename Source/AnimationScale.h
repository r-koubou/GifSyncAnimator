#pragma once

namespace rkoubou::GifSync
{
    enum class AnimationScale
    {
        Default = 1,
        Faster2x,
        Faster4x,
        Faster8x,
        Faster16x,
        Faster32x,
        Faster64x,
        Slower2x,
        Slower4x,
        Slower8x,
        Slower16x,
        Slower32x,
        Slower64x,
    };
}
